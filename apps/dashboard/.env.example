# Symlink-style: we read from the repo root .env.local instead.
# Override here if you want app-specific values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
CONTROL_PLANE_URL=http://localhost:8000
