export default function InvestigatePage() {
  return (
    <div className="p-8 max-w-3xl">
      <h1 className="text-2xl font-semibold tracking-tight mb-2">Investigate</h1>
      <p className="text-sm text-muted-foreground">
        Phase 3+ feature — ad-hoc DuckDB query interface. Useful when you want
        to poke around the telemetry without spinning up an agent.
      </p>
    </div>
  );
}
